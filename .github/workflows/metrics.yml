# DISABLED - Memerlukan METRICS_TOKEN di Repository Secrets
# Uncomment dan setup token jika ingin menggunakan advanced metrics

name: GitHub Metrics

 on:
#   # Schedule updates (each 6 hours)
  schedule: [{cron: "0 */6 * * *"}]
#   # Lines below let you run workflow manually and on each commit
   workflow_dispatch:
  push: {branches: ["master", "main"]}

 jobs:
   github-metrics:
    runs-on: ubuntu-latest
     permissions:
       contents: write
     steps:
       - uses: lowlighter/metrics@latest
         with:
#           # Your GitHub token - HARUS DIBUAT DI REPOSITORY SECRETS
           token: ${{ secrets.METRICS_TOKEN }}

#           # Options
           user: rahmanS1d1q
           template: classic
          base: header, activity, community, repositories, metadata
           config_timezone: Asia/Jakarta

#           # Plugin: Isometric calendar
           plugin_isocalendar: yes
           plugin_isocalendar_duration: half-year

#           # Plugin: Languages
          plugin_languages: yes
          plugin_languages_analysis_timeout: 15
          plugin_languages_categories: markup, programming
          plugin_languages_colors: github
          plugin_languages_limit: 8
          plugin_languages_recent_categories: markup, programming
           plugin_languages_recent_days: 14
           plugin_languages_recent_load: 300
           plugin_languages_sections: most-used
           plugin_languages_threshold: 0%

#           # Plugin: Activity
           plugin_activity: yes
           plugin_activity_days: 14
           plugin_activity_filter: all
           plugin_activity_limit: 5
           plugin_activity_load: 300
           plugin_activity_visibility: all

#           # Plugin: Achievements
           plugin_achievements: yes
           plugin_achievements_display: detailed
           plugin_achievements_secrets: yes
           plugin_achievements_threshold: C
